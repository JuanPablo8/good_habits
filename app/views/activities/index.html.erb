<h1>Listing activities</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
  </tr>

  <% @activities.each do |activity| %>
    <tr>
      <td><%= activity.title %></td>
      <td><%= link_to 'Show', activities_path(activity) %></td>
    </tr>
  <% end %>
</table>